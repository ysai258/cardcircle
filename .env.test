# Test environment.
#
# Committed on purpose so `npm test` works from a fresh clone. Everything
# here is local and disposable — this key protects nothing and must never be
# reused for development or production.
DATABASE_URL="postgresql://cardcircle:cardcircle_local_dev@localhost:55433/cardcircle_test"
APP_MASTER_KEY="dGVzdC1vbmx5LWtleS1ub3QtZm9yLXJlYWwtdXNlISE="
NODE_ENV="test"
